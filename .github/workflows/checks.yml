name: SHACL validation

on:
  workflow_call:
  push:
    paths:
      - 'ontology-combined.ttl'
      - 'ontology.ttl'
      - 'shapes.ttl'
  pull_request:
    paths:
      - 'ontology-combined.ttl'
      - 'ontology.ttl'
      - 'shapes.ttl'
jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v2

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: SHACL validation
        run: |
          uv run python tools/python/checks/shacl.py ontology-combined.ttl src/quality-checks/shacl-shacl.ttl
