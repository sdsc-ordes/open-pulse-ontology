name: SHACL Validation Tests

on:
  push:
    branches: [main, master, develop, ontology-revision]
  pull_request:
    branches: [main, master, develop, ontology-revision]
  workflow_dispatch:

jobs:
  validate-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Install uv
        uses: astral-sh/setup-uv@v2

      - name: Install uv
        uses: astral-sh/setup-uv@v2

      - name: Install uv
        uses: astral-sh/setup-uv@v2

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run SHACL validation test suite
        run: |
<<<<<<< HEAD:.github/workflows/validate-tests.yaml
<<<<<<< HEAD:.github/workflows/validate-tests.yaml
          uv sync --frozen --extra dev
=======
>>>>>>> a3cc4f3 (refactor: make sure gh actions also uses uv):.github/workflows/validate-tests.yml
=======
          uv sync --frozen --extra dev
>>>>>>> ba55ed5 (fix: make all actions use uv, add explicit validation step in validate-test action):.github/workflows/validate-tests.yml
          uv run python tools/python/checks/test_validation.py

      - name: Upload test results on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: validation-test-results
          path: |
            example/test_*.ttl
            ontology-combined.ttl
