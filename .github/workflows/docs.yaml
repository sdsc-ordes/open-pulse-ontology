name: SHACL Documentation Generation

on:
  push:
    branches:
      - main
    branches:
      - "**"
      - "!docs" # Exclude docs branch to prevent loops
    paths:
      - "ontology-combined.ttl"
      - "ontology.ttl"
      - "shapes.ttl"
    tags:
      - "v*"
  pull_request:
    branches:
      - main
    paths:
      - "ontology-combined.ttl"
      - "ontology.ttl"
      - "shapes.ttl"

permissions:
  contents: write

env:
  DOCS_BRANCH: docs

jobs:
  generate-docs:
    runs-on: ubuntu-latest
    outputs:
      should_deploy: ${{ steps.check-deploy.outputs.should_deploy }}
      version_type: ${{ steps.version-info.outputs.type }}
      version_name: ${{ steps.version-info.outputs.name }}
      commit_hash: ${{ steps.version-info.outputs.commit }}
      commit_date: ${{ steps.version-info.outputs.date }}
      commit_author: ${{ steps.version-info.outputs.author }}
      commit_message: ${{ steps.version-info.outputs.message }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check if should deploy
        id: check-deploy
        run: |
          if [ "${{ github.event_name }}" == "pull_request" ]; then
            echo "should_deploy=false" >> $GITHUB_OUTPUT
            echo "Pull request - will build but not deploy"
          else
            echo "should_deploy=true" >> $GITHUB_OUTPUT
            echo "Push event - will build and deploy"
          fi

      - name: Determine version info
        id: version-info
        run: |
          # Get commit info
          COMMIT_HASH=$(git rev-parse --short=7 HEAD)
          COMMIT_DATE=$(git log -1 --format=%cs)
          COMMIT_AUTHOR=$(git log -1 --format=%an)
          # Escape quotes and limit message length
          COMMIT_MESSAGE=$(git log -1 --format=%s | head -c 100 | sed 's/"/\\"/g')
          
          # Determine if this is a tag or branch
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            VERSION_TYPE="release"
            VERSION_NAME="${{ github.ref_name }}"
          else
            VERSION_TYPE="branch"
            VERSION_NAME="${{ github.ref_name }}"
          fi
          
          echo "type=$VERSION_TYPE" >> $GITHUB_OUTPUT
          echo "name=$VERSION_NAME" >> $GITHUB_OUTPUT
          echo "commit=$COMMIT_HASH" >> $GITHUB_OUTPUT
          echo "date=$COMMIT_DATE" >> $GITHUB_OUTPUT
          echo "author=$COMMIT_AUTHOR" >> $GITHUB_OUTPUT
          echo "message=$COMMIT_MESSAGE" >> $GITHUB_OUTPUT
          
          echo "----------------------------------------"
          echo "Version type: $VERSION_TYPE"
          echo "Version name: $VERSION_NAME"
          echo "Commit hash: $COMMIT_HASH"
          echo "Commit date: $COMMIT_DATE"
          echo "Commit author: $COMMIT_AUTHOR"
          echo "Commit message: $COMMIT_MESSAGE"
          echo "----------------------------------------"

      - name: Set up Python and install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r tools/python/requirements.txt

      - name: Set up Java 11
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "11"

      - name: Install Graphviz
        run: |
          sudo apt-get update
          sudo apt-get install -y graphviz

      - name: Set DOT_PATH environment variable
        run: echo "GRAPHVIZ_DOT=/usr/bin/dot" >> $GITHUB_ENV

      - name: Download SHACL Play CLI
        run: |
          wget https://github.com/sparna-git/shacl-play/releases/download/0.11.4/shacl-play-app-0.11.4-onejar.jar -O shacl-play-cli.jar

      - name: Generate Documentation
        run: |
          mkdir -p generated-docs
          java -jar shacl-play-cli.jar \
            doc \
            -d \
            -i ontology-combined.ttl \
            -l en \
            -o generated-docs/index.html

      - name: Generate Ontology Diagram
        run: |
          java -jar shacl-play-cli.jar \
            draw \
            -i ontology-combined.ttl \
            -o generated-docs/ontology.svg

      - name: Fix SVG namespace issue
        run: |
          sed -i 's/ xmlns=""//g' generated-docs/ontology.svg generated-docs/index.html
          # Remove XML declaration from inline SVG (breaks HTML rendering)
          sed -i 's/<?xml version="1.0" encoding="us-ascii" standalone="no"?>//g' generated-docs/index.html

      - name: Inject Enumerations into HTML
        run: |
          python tools/python/docs/inject_enumerations.py ontology-combined.ttl generated-docs/index.html

      - name: Upload generated documentation as artifact
        uses: actions/upload-artifact@v4
        with:
          name: shacl-documentation
          path: docs/index.html
