name: SHACL Documentation Generation

on:
  push:
    paths:
      - "ontology-combined.ttl"
      - "ontology.ttl"
      - "shapes.ttl"
  pull_request:
    paths:
      - "ontology-combined.ttl"
      - "ontology.ttl"
      - "shapes.ttl"

jobs:
  generate-docs:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python and install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r tools/python/requirements.txt

      - name: Set up Java 11
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "11"

      - name: Install Graphviz
        run: |
          sudo apt-get update
          sudo apt-get install -y graphviz

      - name: Set DOT_PATH environment variable
        run: echo "GRAPHVIZ_DOT=/usr/bin/dot" >> $GITHUB_ENV

      - name: Download SHACL Play CLI
        run: |
          wget https://github.com/sparna-git/shacl-play/releases/download/0.11.4/shacl-play-app-0.11.4-onejar.jar -O shacl-play-cli.jar

      - name: Generate Documentation
        run: |
          java -jar shacl-play-cli.jar \
            doc \
            -d \
            -i ontology-combined.ttl \
            -l en \
            -o docs/index.html

      - name: Generate Ontology Diagram
        run: |
          java -jar shacl-play-cli.jar \
            draw \
            -i ontology-combined.ttl \
            -o docs/ontology.svg

      - name: Fix SVG namespace issue
        run: |
          sed -i 's/ xmlns=""//g' docs/ontology.svg

      - name: Inject Enumerations into HTML
        run: |
          python tools/python/docs/inject_enumerations.py ontology-combined.ttl docs/index.html

      - name: Commit generated docs (for push events only)
        if: github.event_name == 'push'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/index.html
          git add docs/ontology.svg
          git commit -m "Update generated docs" || echo "Nothing to commit"
          git push

      - name: Upload generated documentation as an artifact
        uses: actions/upload-artifact@v4
        with:
          name: shacl-documentation
          path: docs/index.html
